# Fedora T540p — Core Configuration

## Network
Document interface names, addressing, gateway, DNS, routes, and connection profile.

## Services
Document only services belonging to the Core Layer.

## Security
Document firewall, SSH, SELinux, and host security controls.

## Virtualization
Document KVM/QEMU/libvirt settings belonging to the Core baseline.

## Container Runtime
Document Docker or equivalent Core runtime configuration.

## Backup Boundary
Do not publish secrets, private keys, tokens, credentials, or sensitive host data.
